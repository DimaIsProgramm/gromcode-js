export const getInputElement = titleElem => {
  const myElem = document.querySelector('.title');
  console.dir(myElem);
  return myElem;
};

export const getInputElement = inputElem => {
  const myInput = document.querySelector('input').querySelector('text');
  console.dir(myInput);
  return myInput;
};
